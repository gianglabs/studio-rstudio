[workspace]
channels = ["conda-forge", "bioconda"]
name = "data-sw-rstudio"
platforms = ["linux-64"]
version = "0.1.0"

[tasks]

[dependencies]
r-base = { version = "4.6.1.*", channel = "conda-forge" }
python = { version = "3.12.*", channel = "conda-forge" }
podman = { version = "5.8.3.*", channel = "conda-forge" }
zstd = { version = "1.5.7.*", channel = "conda-forge" }
pigz = { version = "2.8.*", channel = "conda-forge" }
