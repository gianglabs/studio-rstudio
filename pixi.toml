[workspace]
channels = ["conda-forge", "bioconda"]
name = "rstudio"
platforms = ["linux-64"]
version = "0.1.0"

[activation.env]
CONTAINERS_CONF = "${CONDA_PREFIX}/etc/containers/containers.conf"

[tasks]

[dependencies]
podman = "<5"
zstd
